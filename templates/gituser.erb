[user]
    name = <%= print("Git Name: "); STDOUT.flush; STDIN.gets.chomp %>
    email = <%= print("Git Email: "); STDOUT.flush; STDIN.gets.chomp %>
[github]
    user = tfe
    token = <%= print("GitHub Token: "); STDOUT.flush; STDIN.gets.chomp %>
    
