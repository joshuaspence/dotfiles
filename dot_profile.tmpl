if test -n "${BASH_VERSION}" && test -f ~/.bashrc; then
  . ~/.bashrc
fi

{{- $base := print .chezmoi.sourceDir "/" }}
{{ range $file := glob (joinPath .chezmoi.sourceDir "shell/environment/*.sh") }}
# source={{ replace $base "" $file }}
{{ include $file -}}
{{ end -}}
