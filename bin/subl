#!/bin/bash

# This is just a shortcut to sublime-text-2, because `sublime-text-2` is way too
# long to be
if [[ ! "$1" || "$1" == "-h" || "$1" == "--help" ]]; then cat <<HELP

HELP
exit
fi

/usr/bin/sublime-text-2