---
name: 'Lint'
on:
  pull_request: {}
  push:
    branches:
      - 'master'
jobs:
  shellcheck:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v4'
      - uses: './.github/actions/chezmoi'
      - run: 'mkdir .target'
      - run: 'chezmoi archive --exclude externals | tar --extract --directory .target'
      - uses: 'ludeeus/action-shellcheck@2.0.0'

  yamllint:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v4'
      - uses: 'ibiqlik/action-yamllint@v3.1.1'
