{
    /*
     * When true, the first row is treated as the header row. Set to false to
     * always sniff for headers (sniffing isn't perfect!).
     */
    "assume_headers": false,

    /*
     * If false, defaults to the OS line separator.
     */
    "line_sep": false
}
