#/
## Configuration file for {@link http://curl.haxx.se/ curl}.
##
## @author Joshua Spence
## @file   ~/.curlrc
#\

## Automatically set the previous URL when it follows a "Location:" header.
referer = ";auto"

## Use the specified certificate directory to verify the peer.
capath = /etc/ssl/certs

## Follow redirects.
location

## Use "Content-Disposition" filename instead of URL.
remote-header-name

## Request compression.
compressed

## Don't show/download error document, only error code.
fail

## FTP setup. @{{{
ftp-create-dirs
ftp-ssl
ftp-pasv
ftp-method = nocwd
## @}}}

## Load a `~/.netrc` for login name and password if it exists.
netrc-optional

## When using "-o", create the leading directories if necessary.
create-dirs
