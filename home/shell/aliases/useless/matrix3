# Don't overwrite alias.
command -v matrix3 >/dev/null && return

##
# Cool "The Matrix" type screens.
#
# @link @todo
#
alias matrix3='tr -c "[:digit:]" " " </dev/urandom |
    dd cbs=$(tput cols) conv=lcase,unblock |
    GREP_COLOR="1;32" grep --color "[^ ]"'
