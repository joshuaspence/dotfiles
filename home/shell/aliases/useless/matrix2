##
# Cool "The Matrix" type screens.
#
# @link @todo
#
alias matrix2="(
    echo -n -e '\e[32m'
    while true; do
        echo -n -e \"\e[\$((\$RANDOM % 2 + 1))m\"
        tr -c '[:print:]' ' ' </dev/urandom | dd count=1 bs=50 2>/dev/null
    done
)"
