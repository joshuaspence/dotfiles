# Unset environment variables.
unset MANPATH

# We need the path functions!
[ -r "$HOME/.shell/functions/path/prepend" ] && source "$HOME/.shell/functions/path/prepend" || return 1

# ~/bin should be in the front of PATH if it exists.
if [ -d "$HOME/bin" ]; then
    prepend-path PATH "$HOME/bin"
fi

# ~/man should be in the front of MANPATH if it exists.
if [ -d "$HOME/man" ]; then
    MANPATH="$HOME/man:"
fi

# Export environment variables.
export PATH
export MANPATH
