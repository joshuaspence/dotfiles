#!/bin/sh

# Store the name of the shell.
export SHELL=$0

# Check if this is an interactive shell.
if [[ $- == *i* ]]; then
    export IS_INTERACTIVE=1
fi

# Check is this is an SSH connection.
if [ -n $SSH_TTY ] || [ -n $SSH_CLIENT ] || [ -n $SSH_CONNECTION ]; then
    export IS_REMOTE=1
fi
