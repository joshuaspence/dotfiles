# NOTE: `bind -p` can be used to show current bindings.

$include /etc/inputrc

# Show automatically how parentheses (and other matching delimiters) match in
# the text. Whenever you type a self-inserting character that is a closing
# delimiter, the cursor moves momentarily to the location of the matching
# opening delimiter, provided that is on the screen.
set blink-matching-paren on

# Make tab autocomplete regardless of filename case.
set completion-ignore-case on

# Don't echo control characters (such as `^C`). Note this only works for the
# command line itself, not if already running a command.
set echo-control-characters off

# Immediately add a trailing slash when autocompleting symlinks to directories.
set mark-symlinked-directories on

# Do not autocomplete hidden files unless the pattern explicitly begins with a
# dot.
set match-hidden-files off

# Use an internal `more`-like pager to display a screenful of possible
# completions at a time.
set page-completions on

# Alters the default completion behavior when inserting a single match into
# the line.
set skip-completed-text on

"\e[A": history-search-backward
"\e[B": history-search-forward
"\e[3;3~": kill-word

$if Bash
  Space: magic-space
$endif
