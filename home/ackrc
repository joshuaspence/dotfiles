##
## My configuration file for {@link http://betterthangrep.com/ ack-grep}.
##
## @author Joshua Spence
## @file   ~/.ackrc
##


## Operate on all files, regardless of type.
#--all

## Always color, even if piping to a another program.
--color

## Specifies the color of the filename when it's printed in `--group` mode. By
## default, it's "bold green".
#--color-filename="bold green"

## Specifies the color of the line number when printed in `--color` mode. By
## default, it's "bold yellow".
#--color-lineno="bold yellow"

## Specifies the color of the matching text when printed in `--color` mode. By
## default, it's "black on_yellow".
#--color-match="black_on_yellow"

## Print NUM lines (default 2) of context around matching lines.
#--context=2

## Don't follow symlinks.
--nofollow

## Outputs the filetypes that `ack-grep` associates with each file.
#--show-types

## Ignore case in the search strings if the search pattern contains no uppercase
## characters.
--smart-case

## Sorts the found files lexically. Use this if you want your file listings to
## be deterministic between runs of `ack-grep`.
--sort-files

## Print the filename for each match.
--with-filename

## Types.
--type-add=css=.less,.sass,.scss
--type-add=html=.html.erb,.html.haml
--type-add=js=.coffee,.js.erb
--type-add=php=.php.inc
--type-add=ruby=.builder,.haml,.rake,.rspec,.ruby-version
--type-add=ruby=Gemfile,Rakefile

## Add types that `ack-grep` doesn't know about.
--type-set=coffee=.coffee
--type-set=config=.cfg,.conf,.config
--type-set=csv=.csv
--type-set=log=.log
--type-set=json=.json
--type-set=markdown=.md,.markdown
--type-set=puppet=.pp

## Ignore these directories.
--ignore-dir=doc
--ignore-dir=log
--ignore-dir=rdoc
--ignore-dir=tmp
--ignore-dir=vendor

## Disable file types.
--nolog
