#/
## Initialization for interactive non-login
## {@link http://www.gnu.org/software/bash/ bash} shells.
#\

## Pull in any system defaults first so they can be overridden later.
if [[ -f /etc/bash.bashrc && -r /etc/bash.bashrc ]]; then
    source /etc/bash.bashrc
fi

## Source prerequisite function.
command -v source_r >/dev/null || source "${HOME}/.shell/functions/source/source_r.sh"

## Setup shell options.
source_r "${HOME}/.shell/config/"

## Setup the environment.
source_r "${HOME}/.shell/environment/"

## Import command aliases.
source_r "${HOME}/.shell/aliases/"

## Import user-defined functions.
source_r "${HOME}/.shell/functions/"

## Import completion functions.
source_r "${HOME}/.shell/completion/"

export PATH=$PATH:$HOME/.rvm/bin
export PATH=$PATH:$HOME/dotfiles/bin
export PATH=$PATH:$HOME/.composer/vendor/bin
[[ -s "/home/joshua/.gvm/scripts/gvm" ]] && source "/home/joshua/.gvm/scripts/gvm"
