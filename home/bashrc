#/
## @author Joshua Spence
## @file ~/.bashrc
#\

## We need the source_r function.
command -v source_r >/dev/null || source "${HOME}/.shell/functions/source/source_r.sh"

## Setup the environment.
source_r "${HOME}/.shell/environment"

## Import command aliases.
source_r "${HOME}/.shell/aliases"

## Import user-defined functions.
source_r "${HOME}/.shell/functions"

## Set up completion.
source_r "${HOME}/.shell/completion"

## Set up shell options.
source_r "${HOME}/.shell/bash"
