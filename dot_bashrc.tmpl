# If not running interactively, don't do anything.
# See https://unix.stackexchange.com/a/257613.
if [[ $- != *i* ]]; then
  return
fi

{{- $home := print .chezmoi.homeDir "/" }}
{{ range $file := glob (joinPath .chezmoi.sourceDir "shell/{config,completion,functions}/*.{bash,sh}") }}
# {{ replace $home "" $file }}
{{ include $file -}}
{{ end -}}
